#include "main.h"
/**
 *_isdigit - checks is character id a digit.
 *@c: print int
 * Return: 1 for digit 0 else
 */

int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}

else
{
return (0);
}
}

